export class Challenger {
  constructor(id: number, name: string, prono: number, fetard: number) {
    this.id = id;
    this.name = name;
    this.prono = prono;
    this.fetard = fetard;
  }
  id: number;
  name: string;
  prono: number;
  fetard: number;
}